const now = new Date()
//~console.log(now);


const year = now.getFullYear(); 
const month = now.getMonth(); // 0-indexed (January is 0, February is 1, etc.)
const day = now.getDate(); 
const hours = now.getHours(); 
const minutes = now.getMinutes(); 
const seconds = now.getSeconds(); 
const milliseconds = now.getMilliseconds(); 

//console.log(`all method in one frame ${year} ${month} ${day} ${hours} ${minutes} ${seconds} ${milliseconds} `);

// and other option is present in date method like ''get date'' ''set date''  ''get time'' ''set time''  
